# 全国公益法人協会 — Design System v1.3

> 信頼を、よりモダンに伝える。
> 上品なミディアムブルーとクールグレーで再定義したブランド資産ガイドライン。
> Web・印刷物・広報物まで、一貫した佇まいで「公益・一般法人の心強いミカタ」を表現する。

- **Audience**: 公益法人・一般法人の運営担当者
- **Voice**: 誠実 / 専門的 / 親身
- **Updated**: 2026.08 — §16「v1.3 現況追補（財団サイト実測）」を追加し、§03 タイポグラフィを実装基準に書き換え
- **既刊**: 2026.07 — §14「v1.1 アクセシビリティ追補」・§15「v1.2 実装追補（財団サイト適用）」

---

## 01. Brand Foundation — ブランドの輪郭

ロゴのキューブモチーフは、専門家・運営者・法人がつながり「共に問題を解決する」協会の役割を象徴する。シンボルの濃淡ブルーは、公益性の**誠実さ**と、開かれた**透明性**を同時に表現する。

### Voice & Tone — 文体の3原則

| 原則 | 内容 |
| --- | --- |
| **誠実 — 正確に、断定する** | 関連法令・通達に基づく正確な記述を最優先。曖昧な表現や過度な誇張は避け、根拠を明示する。 |
| **明快 — 短く、構造的に** | 1文40字以内を目安。専門用語は初出時に補足。要点→詳細の順で、運営担当者の判断速度を上げる文章に。 |
| **親身 — 「お悩みの方へ」** | 運営の現場感に寄り添う言葉選び。「させていただく」より「いたします」を基本に、距離感のある丁寧語で。 |

---

## 02. Color System — カラーシステム

「ミディアムブルーをベースに、ネイビーとクールグレーで上品に締める」が基本構造。ブルーは信頼と公益性、グレーは静けさと専門性を担う。彩度の高いアクセントは限定的に使用。

### Primary — Navy

| Token | HEX | 用途 |
| --- | --- | --- |
| `--navy-900` | `#0E2147` | 本文・最も濃い見出し（CORE） |
| `--navy-800` | `#15305F` | 背景（ダーク）・バッジ |
| `--navy-700` | `#1B3F77` | 主要見出し・アウトラインボタン |
| `--navy-600` | `#234E91` | — |
| `--navy-500` | `#2B62AE` | ブランドカラー（ロゴ／PRIMARY） |

### Accent — Sky Blue

| Token | HEX | 用途 |
| --- | --- | --- |
| `--blue-500` | `#2F8BD2` | CTAボタン・リンク（CTA） |
| `--blue-400` | `#4FA6E2` | ホバー・アンダーライン |
| `--blue-300` | `#7BC1ED` | — |
| `--blue-200` | `#BFE0F4` | アイコン背景・引用（SOFT） |
| `--blue-100` | `#E6F3FB` | ハイライト背景 |

### Neutral — Cool Gray

| Token | HEX | Token | HEX |
| --- | --- | --- | --- |
| `--ink-950` | `#0B1220` | `--ink-400` | `#A2A9B8` |
| `--ink-900` | `#161E2D` | `--ink-300` | `#C7CCD6` |
| `--ink-800` | `#2A3243` | `--ink-200` | `#E2E5EC` |
| `--ink-700` | `#3F4759` | `--ink-100` | `#F1F3F7` |
| `--ink-600` | `#5A6376` | `--ink-50`  | `#F8F9FB` |
| `--ink-500` | `#7B8497` | `--white`   | `#FFFFFF` |

### Accent — Warm Orange（原則として使わない・例外的にだけ使う）

> ⚠️ **オレンジは「ここだけ」のための色。**
> ブランドの主軸はあくまで Navy + Blue。オレンジは「1画面（1スクリーン）に1ヶ所」を原則とし、**本当に誤読・見落としが許されない一点**に限定して使う。迷ったら使わない、が正解。

| Token | HEX | 用途 |
| --- | --- | --- |
| `--orange-700` | `#B8551E` | 期限超過・エラー註記 |
| `--orange-500` | `#D87C3A` | 「ここだけ」の警告・超限定キャッチ（RARE） |
| `--orange-200` | `#F6D9C2` | 警告ボックスの縁 |
| `--orange-100` | `#FBEEE2` | 警告ノートの底色 |

#### ○ DO — 使ってよい場面
- 誤ると不利益が生じる**期限・締切註記**（「5月1日未提出で認定取消」など）
- 状態色の **Warning**（進捗・エラー以前の警告）
- **見落としてはいけない**一説規定・重要註記のアイコン・引出し線
- オリジナル画像のキャッチを**そのまま転載**するとき（作りださない）

#### × DON'T — 使わない
- **通常のCTA・ボタン**（CTAは Blue 500 / Navy 700）
- **リンク色・見出し色・本文色**には使わない
- **「NEW」「人気」「おすすめ」**などの装飾・誘導バッジ（これらは Navy / Blue）
- **背景、帯、大面積のグラデーション**（オレンジを広げると「安さ」を招く）
- **同一画面に2ヶ所以上**（スクリーンごとに1点まで）

> **チェックリスト：** 使う前に「これは Navy / Blue / Ink で表現できないか？」と自問する。表現できるなら、そちらを選ぶのがこのシステムの規範。オレンジは「使わないと伝わらない」と確信できたときだけ手にする色。

### Semantic — 状態色（緑禁則）

ブランドポリシーとして**緑色は使用しない**。「完了・受付中」などのポジティブな状態は Navy で表現し、注意は Warning、エラーは Danger を使い分ける。

| Token | HEX | 用途 |
| --- | --- | --- |
| `--success` | `#1B3F77` | 完了・受付中（Navy 700 を流用） |
| `--warning` | `#C66B2D` | 注意・期日近接 |
| `--danger`  | `#D04C4C` | エラー・期限超過 |

### Usage Ratio — 配色比率

- **Light Mode（標準）**: 60% White / Ink-50 + 30% Navy + 10% Sky
  本文・コンテンツは Ink-900、見出しは Navy 800〜900。アクセントは CTA とリンクに限定。
- **Dark Hero（強調）**: 70% Navy 900 + 20% Navy 700 + 10% Blue 300
  ヒーロー・セクション区切りで使用。本文は Blue 200 で軽くトーンを乗せる。

---

## 03. Typography — タイポグラフィ

> **v1.3 改訂（2026.08）** — 財団サイト（koueki.or.jp）の実装を実測して書き換えた。v1.0 の型スケール（Display 64px / Body 16px など）は原案であり、**実サイトの値は本節の表が正**。協会サイト（koueki.jp）側の値は別途確認のこと。

和文は `Noto Sans JP`、ヒーローのキャッチコピーだけ `Noto Serif JP`、欧文・算用数字は `Inter` の3書体構成。本文は **15px / 行送り1.8** が基準（v1.0 の 16px は実装で 15px に落ち着いた）。見出しは太さを盛らず、**Navy の濃さの段**で紙面のリズムを作る。

### 3-1. 3書体の役割 — どこで何を使うか

| 書体 | 役割 | 使う場所 |
| --- | --- | --- |
| **Noto Sans JP** | 既定書体。文書全体 | 本文・見出し・UI・バッジ・和暦 |
| **Noto Serif JP** | キャッチコピー専用 | ヒーロー h1 のみ（§15-1） |
| **Inter** | 欧文・算用数字専用 | 英字エイブロー、西暦の日付、金額・件数 |

- ⚠️ **明朝を広げない**。セクション見出し・リード・本文・カード見出しは Sans。明朝はヒーロー h1 の1ヶ所だけ
- ⚠️ **Inter を和文に当てない**。和暦（「令和7年11月17日」）や和文混在の文字列は Noto Sans JP のまま

### 3-2. 実装ウェイト（Web = next/font/google）

| 書体 | 読み込んでいる | 実際に使っている | 備考 |
| --- | --- | --- | --- |
| Noto Sans JP | 300 / 400 / 500 / 700 / 900 | **400 / 500 / 700** | 300・900 は使用箇所ゼロ（転送だけ発生） |
| Noto Serif JP | 500 / 600 | **600** | 700 以上は §15-1 で禁止 |
| Inter | 可変フォント（latin） | **400 / 600** | 可変なので追加ウェイトのコストは無い |

> **規範: 使うウェイトは 3段（400 / 500 / 700）に絞る。** 見出しは 700 で止め、**900（Black）は財団サイトでは使わない**（v1.0 §03 の Display 900 は協会サイト側の規定として残す）。`font-semibold`(600) は Serif のキャッチと Inter のエイブロー専用で、和文 Sans には使わない。
>
> 実装メモ: フォント読み込みの `weight` 指定から `300` と `900` を外してよい（使用箇所が無く、転送だけ発生している）。表示は変わらない。

### 3-3. 型スケール（財団サイト実測値）

| スタイル | 書体 / ウェイト | Desktop | Mobile | 実装クラス |
| --- | --- | --- | --- | --- |
| **Hero H1**（キャッチ） | Serif 600 | 44 / 1.4 / +.02em | 32 / 1.45 | `font-serif text-[2rem] md:text-[2.75rem]` |
| **Section H2** | Sans 700 | 32 / 1.3 / -.005em | 24 | `text-2xl md:text-[2rem]` |
| **Contact H2**（ダーク面） | Sans 700 | 28 | 24 | `text-2xl md:text-[1.75rem]` |
| **Card / Timeline H3** | Sans 700 | 18 / 既定（1.4〜1.55） | 18 | `text-lg`（コラムカードのみ `leading-snug`） |
| **Lead**（ヒーロー補足） | Sans 500 | 16 / 1.8 | 16 | `font-medium leading-[1.8]` |
| **Body** | Sans 400 | 15 / 1.8 | 15 | `text-[15px] leading-[1.8]` |
| **Small**（日付・注記・リンク） | Sans 400/500 | 14 / 1.43（本文的な段落は 1.8） | 14 | `text-sm` / `text-sm leading-[1.8]` |
| **Badge / Eyebrow** | Sans 500 / Inter 600 | 12 | 12 | `text-xs` / `.eyebrow-en` |
| **記事本文** | Sans 400 | 16（typography プラグイン既定） | 同 | `.prose`（palt 解除） |

- 切り替えの境界は **`md`（768px）**。§14-5 の「〜640px」より広い側で切り替えている
- 縮小するのは **Hero H1 と Section H2 の2つだけ**。他は1段で通す（サイズ段を増やすほど崩れる）
- Display（64px）相当は財団サイトには存在しない。最大文字は Hero H1 の 44px
- v1.0 の Heading 1（Sans 700 / 44px）は財団サイトでは未使用。44px の座は Serif のキャッチが占めている

### 3-4. 和文・欧文・数字の組み合わせ

1. **英字エイブロー** = `.eyebrow-en`（Inter 600 / 12px / +.26em / 大文字 / accent-600）。先頭に 2rem のグラデ短線（navy-700 → accent-300、「歩みの線」と同じ線言語）。**ダーク面では `!text-accent-300` に差し替える**（navy-900 の上で accent-600 はコントラスト不足）
2. **西暦の日付・件数・金額は `font-en tabular-nums`**。Tailwind の `tabular-nums` を使い、`font-feature-settings:'tnum'` を手書きしない
3. **和暦は `tabular-nums` のみ**（`font-en` を付けない）。沿革の「令和7年11月17日」がこの形
4. 組織名は和文表記で統一。英名（Japan Foundation for Public Interest Support / JFPS）はヒーローのエイブローとフッターだけで使う

### 3-5. 約物（palt）の運用

`body` に `font-feature-settings: 'palt' 1`、`.prose` で `normal` に戻す。**見出し・UI・短文は詰め、CMS の長文記事は標準送り** — v1.1 §14-6 の推奨をそのまま実装した形。

```css
body   { font-feature-settings: 'palt' 1; }
.prose { font-feature-settings: normal; }
```

### 3-6. Font Stack — Web は stack を手書きしない

Next.js 側は next/font が生成する CSS 変数をそのまま使う。

```css
/* Tailwind v4 の @theme inline での宣言例 */
--font-sans:  var(--font-noto-sans-jp);   /* = "Noto Sans JP", "Noto Sans JP Fallback" */
--font-serif: var(--font-noto-serif-jp);
--font-en:    var(--font-inter);
/* Tailwind の既定書体も張り替わる: --default-font-family: var(--font-noto-sans-jp) */
```

- 使えるユーティリティは **`font-sans`（既定・書かなくてよい）/ `font-serif` / `font-en`** の3つ
- フォールバックは next/font が作る metric-adjusted な `local(Arial)` 1枚だけ（`size-adjust:104.76%`）。**Hiragino / Yu Gothic は Web の stack に入らない**。woff2 が落ちた場合は OS の代替に委ねる設計
- Noto Sans JP / Serif JP は CJK の unicode-range 分割 CSS（各124スライス）で配信される。`subsets:['latin']` の指定でも**和文グリフは読み込まれる**（Google がスライス済み CSS を返すため）。ヒーロー可視域のフォントは4本 preload される
- **印刷物・スライド・Next.js 以外の媒体**では次の stack を使う:

```css
--ff-jp:       "Noto Sans JP", "Hiragino Kaku Gothic ProN", "Yu Gothic", system-ui, sans-serif;
--ff-jp-serif: "Noto Serif JP", "Hiragino Mincho ProN", "Yu Mincho", serif;
--ff-en:       "Inter", "Noto Sans JP", system-ui, sans-serif;
```

- `--ff-mono`（JetBrains Mono）は財団サイトでは未使用。等幅が必要なら Tailwind 既定の mono をそのまま使う

## 04. Spacing & Grid — 余白とグリッド

ベースは **4px** グリッド。要素間 8 / 16 / 24 / 32px、セクション間 64 / 96px が基準。コンテンツ最大幅は 1240px、本文カラムは 720px を上限に読みやすさを確保。

### Spacing Scale

| Token | px |
| --- | --- |
| `--s-1` | 4px |
| `--s-2` | 8px |
| `--s-3` | 12px |
| `--s-4` | 16px |
| `--s-5` | 24px |
| `--s-6` | 32px |
| `--s-7` | 48px |
| `--s-8` | 64px |
| `--s-9` | 96px |

### Grid

- **12 columns** / gutter 16px
- **max-width**: 1240px
- **side padding**: 32px

---

## 05. Radius & Shadow — 角丸とシャドウ

角丸は4段階＋ピル形。影は控えめな4段階で、UI要素の階層をやさしく示す。強い影は使わない（公的な品位を損なうため）。

### Border Radius

| Token | px | 用途 |
| --- | --- | --- |
| `--r-sm` | 6px | Input・Tag |
| `--r-md` | 10px | Card・Image |
| `--r-lg` | 16px | Hero・Modal |
| `--r-xl` | 24px | — |
| `--r-pill` | 999px | Badge・Btn |

### Shadow

| Token | 用途 |
| --- | --- |
| `--sh-1` | 静かな浮き / Input focus（`0 1px 2px rgba(14,33,71,.06), 0 1px 1px rgba(14,33,71,.04)`） |
| `--sh-2` | Card hover（`0 4px 12px rgba(14,33,71,.08), 0 2px 4px rgba(14,33,71,.04)`） |
| `--sh-3` | Hero・Modal（`0 12px 32px rgba(14,33,71,.12), 0 4px 8px rgba(14,33,71,.06)`） |
| `--sh-4` | Floating CTA（`0 24px 60px rgba(14,33,71,.18), 0 8px 16px rgba(14,33,71,.08)`） |

---

## 06. Iconography — アイコン

- 線幅 **1.6px**・端は **round** の細身ストロークアイコン
- サイズは **20 / 24 / 32px** を基本
- **塗りは使用しない**（線のみ）
- 色は `Ink-700` または `Navy-700`
- 背景に `Blue-100` の角丸正方形を敷くとカード見出しに最適

**標準アイコン例**: shield / document / expert / card / chat / search / calendar / building / clock / archive / filter / mail

---

## 07. Components — コンポーネント

### Buttons — 主要CTA

| クラス | 役割 |
| --- | --- |
| `.btn-primary` | CTA（Blue 500、ピル形 + 控えめなブルーシャドウ） |
| `.btn-navy` | 主要動線（Navy 700） |
| `.btn-outline` | セカンダリ（Navy 700の枠線） |
| `.btn-ghost` | 補助（Ink 100） |
| `.btn-link` | テキストリンク（Blue 400 アンダーライン） |

サイズ: `.btn-sm`（13px / padding 9×18）/ 標準（15px / 14×28）/ `.btn-lg`（17px / 18×36）

### Eyebrow & Badge

- `.badge-pill`（Navy 800 + 白文字、ヒーロー・セクション冒頭で使用）
- `.badge-pill.outline`（透明地 + Navy 800 枠線）
- `.badge-pill.accent`（Blue 500 + 白文字）
- `.eyebrow-en`（Inter 600、字間 +.26em、Blue 500、両端に短い線）

### Cards — サービス紹介

- 白背景 + Ink 200 1px ボーダー + `--r-lg` 角丸 + `--s-5` パディング
- ホバー時に `--sh-3` シャドウ + `translateY(-2px)`
- アイコンは Blue 100 背景の 44×44 角丸ボックス、Blue 500 のストローク

### Stat Cards — 数値・実績

- 背景: `linear-gradient(135deg, var(--navy-800), var(--navy-700))`
- 数値: Inter 800、44px、白→Blue 200のグラデーションテキスト
- 右下にラジアルグラデーションの装飾円

### Forms

- Input/Select/Textarea: 1.5px Ink 200 ボーダー / `--r-sm` / 12×14 padding
- フォーカス時: `border-color: var(--blue-500)` + `box-shadow: 0 0 0 4px rgba(47,139,210,.15)`
- Label: 13px / weight 700 / Navy 800
- Checkbox: チェック時 Blue 500

---

## 08. Hero Patterns — ヒーローパターン

白基調を原則。背景はピュアホワイト＋淡いブルーのソフトグロー、内部に細いグリッド罫を控えめに敷く。

### 構成順（縦積み）
ネイビー縁取りバッジ → 英字エイブロー → 大見出し（ネイビー） → 補足文 → オレンジのキャッチピル → CTA。右側にビジュアルを配置。

### バリエーション
- **Primary Hero (White)**: 標準。白背景 + キューブモチーフ等のビジュアル。
- **Secondary Hero (Editorial)**: 月刊「公益」など記事・お知らせ用。Navy 800 のステージタグ。

### 構成原則
1. **バッジ → エイブロー → 見出し**: 読み手の課題を呼び込む語（「お悩みの方へ」）→ 英字キー → 商品名 / 見出し、の3段重ね。
2. **価格 / 数字を強調**: 本文中の金額や数値は Inter ボールドで強調。グラデーションテキストで視認性を上げる。
3. **ビジュアル枠は1/3〜1/2**: 右半分にビジュアル枠を確保。キューブモチーフ・写真・誌面サムネのいずれかを使い分ける。

---

## 09. Logo Usage — ロゴ使用ルール

ロゴは協会の最重要資産。最小サイズ・余白・配色を守り、改変は行わない。

### Clear Space — 周囲の余白
- 最小余白 = キューブシンボルの高さ `x`
- ロゴ周囲には、シンボル（キューブ部分）の高さと等しい余白 `x` を必ず確保。他要素・写真・テキストはこの領域に侵入させない。

### 最小サイズ
- **Web**: 高さ **32px 以上**（キューブ部分の視認性を担保）
- **印刷**: 高さ **10mm 以上**
- 名刺・ファビコン用に、シンボルのみの単色版も使用可

### Background Rule — 必ず白背景の上に配置

> ⚠️ ロゴは**必ず白（#FFFFFF）の地の上**に配置する。**白抜き（ノックアウト／反転）・くり抜きは不可**。色面・写真・グラデーションの上に直接置きたい場合は、ロゴを白い長方形プレート（角丸 4px、十分なクリアスペース）に載せ、そのプレートを背景に重ねる。

| パターン | 配置方法 |
| --- | --- |
| A. 標準 | 白背景にそのまま配置 |
| B. ダーク面 | 白プレートに載せる |
| C. 写真・色面 | 白プレートに載せる |

### Do & Don't

**✓ DO**
- 白背景にそのまま配置（標準）
- ダーク面では白プレートに載せる
- 十分なクリアスペースを確保

**✕ DON'T**
- 白抜き（ノックアウト／反転）にしない
- ブレンドモード等で背景に溶け込ませない
- 縦横比を変えて変形しない
- ブランド外の色相に変更しない
- 色面・写真の上に直接重ねない
- ロゴをくり抜き処理にしない

---

## 10. Global Navigation — グローバルナビゲーション

上段に連絡先・電話番号・利用法人ポータル動線（ユーティリティ列）、下段にメインカテゴリを配置。サブメニューはメガメニュー形式。

### メインカテゴリ（7つ固定）
サービス／私たちの想い／会社概要／渋谷賞／出版物／検定試験／採用情報

### 構造原則
1. **ユーティリティ列を上段に**: 電話番号・受付時間・利用法人様ポータル動線は最上段に固定し、再訪ユーザーの導線を最短に。
2. **メインは7カテゴリまで**: 追加時は統合を優先。
3. **メガメニューで階層を吸収**: 深い階層は1段で展開。サービスのサブ項目はカード型で並列表示し、優先順位を視覚化。

---

## 11. News & Lists — お知らせ・セミナー一覧

「日付 → カテゴリ → タイトル」の3段組リストが標準。

- 日付: Inter Tabular（`font-feature-settings: 'tnum'`）、Ink 500
- カテゴリ: Navy のピル（重要）／ Blue 500（メディア）／ Blue 100（分野タグ）
- タイトル: 本文ウェイト、Ink 900

### Category Tags

| 種類 | スタイル |
| --- | --- |
| お知らせ | Navy 700 + 白文字 |
| メディア掲載 | Blue 500 + 白文字 |
| 会計 / 法人運営 / 税務 | Blue 100 + Navy 700 |
| ZOOM配信 / 会場開催 | Ink 100 + Ink 700 |
| 受付中 | Navy 700 + 白文字 |
| 締切間近 | Warning（Orange 600）+ 白文字 |

---

## 12. Sub-brand Cards — 関連事業・サブブランド

シェアコモン200・月刊公益オンライン・全国公益ポータルZ・公益法人会計検定試験・渋谷賞などを共通フォーマットのカードで並列展開。

### カード構造
- アスペクト比 4:3 のビジュアルゾーン（カテゴリ別グラデーション）
- 左上に英字カテゴリラベル（10px / +.2em / weight 600）
- 中央にサブブランド名（大見出し）
- 下部に説明文（14px strong + 12px muted）

### カテゴリ別配色
- **CORE SERVICE**（シェアコモン200）: Navy 800 → Navy 600
- **PUBLICATION**（月刊「公益」）: Blue 100 → White
- **CERTIFICATION**（検定試験）: Ink 100 → White
- **PORTAL**（全国公益ポータルZ）: Navy 900 → Navy 700

### Banner — 帯バナー（フッター上部用）
- ダーク版: Navy 800 → Navy 600 グラデ + 白文字
- ライト版: 白背景 + Ink 200 ボーダー + Blue 100 アイコンボックス

---

## 13. Contact Block — お問合せブロック

「電話番号 → 受付時間 → 問合せフォーム」の3点セットを基本。電話番号は **Inter / Tabular** で大きく組み（36px / weight 800）、視認性とタップしやすさを最優先。フッター上部・サービスページ最下部で必ず設置。

### 構造（2カラム）
- **左**: Navy 900 → Navy 700 グラデ背景 + 白文字
  - CONTACT エイブロー → 見出し → 説明 → 電話番号（アイコン + Inter 36px）→ 受付時間
- **右**: 白背景
  - Primary CTA（新規ご利用）→ Outline CTA（資料請求）→ Link（その他フォーム）

---

## CSS Tokens — 一覧（参考）

```css
:root {
  /* Brand */
  --navy-900: #0E2147;
  --navy-800: #15305F;
  --navy-700: #1B3F77;
  --navy-600: #234E91;
  --navy-500: #2B62AE;
  --blue-500: #2F8BD2;
  --blue-400: #4FA6E2;
  --blue-300: #7BC1ED;
  --blue-200: #BFE0F4;
  --blue-100: #E6F3FB;

  /* Accent (例外使用) */
  --orange-700: #B8551E;
  --orange-600: #C66B2D;
  --orange-500: #D87C3A;
  --orange-400: #E59458;
  --orange-200: #F6D9C2;
  --orange-100: #FBEEE2;

  /* Neutrals */
  --ink-950: #0B1220;
  --ink-900: #161E2D;
  --ink-800: #2A3243;
  --ink-700: #3F4759;
  --ink-600: #5A6376;
  --ink-500: #7B8497;
  --ink-400: #A2A9B8;
  --ink-300: #C7CCD6;
  --ink-200: #E2E5EC;
  --ink-100: #F1F3F7;
  --ink-50:  #F8F9FB;
  --white:   #FFFFFF;

  /* Semantic（緑禁則） */
  --success: #1B3F77;
  --warning: #C66B2D;
  --danger:  #D04C4C;

  /* Spacing (4pt base) */
  --s-1: 4px;  --s-2: 8px;   --s-3: 12px; --s-4: 16px; --s-5: 24px;
  --s-6: 32px; --s-7: 48px;  --s-8: 64px; --s-9: 96px;

  /* Radius */
  --r-sm: 6px; --r-md: 10px; --r-lg: 16px; --r-xl: 24px; --r-pill: 999px;

  /* Shadows */
  --sh-1: 0 1px 2px rgba(14,33,71,.06), 0 1px 1px rgba(14,33,71,.04);
  --sh-2: 0 4px 12px rgba(14,33,71,.08), 0 2px 4px rgba(14,33,71,.04);
  --sh-3: 0 12px 32px rgba(14,33,71,.12), 0 4px 8px rgba(14,33,71,.06);
  --sh-4: 0 24px 60px rgba(14,33,71,.18), 0 8px 16px rgba(14,33,71,.08);

  /* Type（Web は next/font 由来の変数を使う → §03-6） */
  --ff-jp: "Noto Sans JP", "Hiragino Kaku Gothic ProN", "Yu Gothic", system-ui, sans-serif;
  --ff-jp-serif: "Noto Serif JP", "Hiragino Mincho ProN", "Yu Mincho", serif; /* ヒーロー h1 のみ */
  --ff-en: "Inter", "Noto Sans JP", system-ui, sans-serif;
  --ff-mono: "JetBrains Mono", ui-monospace, monospace; /* 財団サイトでは未使用 */
}
```

---

## 14. v1.1 アクセシビリティ追補 — Accessibility Addendum

> **Updated 2026.07** — v1.0 の配色規定を WCAG 2.1 AA 基準で実測監査した結果に基づく追補。既存本文（§01〜13）は改変せず、本章の規定が優先する。

### 14-1. コントラスト補正（実測値つき）

v1.0 規定のうち、白地・白文字との組み合わせで AA（通常文字 4.5:1 / 大文字 3:1）を満たさないものを以下のとおり補正する。

| 対象 | v1.0 規定（実測比） | v1.1 補正（実測比） |
| --- | --- | --- |
| CTAボタン地＋白文字 | Blue 500 `#2F8BD2`（**3.66 ✕**） | **`--blue-600: #2276BE` を新設**（4.77 ✓）。Blue 500 は 24px 以上の大型要素・アイコン・装飾専用に格下げ |
| テキストリンク（白地） | Blue 400 `#4FA6E2`（**2.66 ✕**） | **Navy 600 `#234E91`**（8.17 ✓）。Blue 400 はホバー下線・ダーク面上のテキスト専用 |
| Caption・日付（13px） | Ink 500 `#7B8497`（**3.76 ✕**） | **Ink 600 `#5A6376`**（6.03 ✓）。Ink 500 は無効状態・装飾罫専用 |
| 警告バッジ地＋白文字 | Orange 600 `#C66B2D`（**3.79 ✕**） | **Orange 700 `#B8551E`**（4.82 ✓） |
| エラー文字（白地） | `#D04C4C`（**4.36 ✕**） | **`--danger: #C24444`**（5.00 ✓） |

Navy 系は監査で全項目良好（navy-700 地×白文字 10.4 / 本文 ink-900 16.7 / Dark Hero の blue-200×navy-900 11.4）。補正はアクセント側のみ。

```css
/* v1.1 追加・変更トークン */
--blue-600: #2276BE;  /* CTA地（白文字）の標準 */
--danger:   #C24444;  /* v1.0 の #D04C4C を置換 */
```

### 14-2. §08 ヒーロー構成の修正 — オレンジのキャッチピル

v1.0 §08 は「オレンジのキャッチピル」を標準構成に含めているが、これは §02 の「オレンジは例外の色・装飾/誘導バッジに使わない」と矛盾する。**標準構成からオレンジピルを削除**し、掲載できるのは次の条件をすべて満たす場合のみとする:

1. 期限・締切など「見落とすと不利益が生じる」訴求であること（§02 DO と同一基準）
2. その画面でオレンジの使用が他に 1 ヶ所もないこと
3. 通常の販促キャッチ（「人気」「おすすめ」等）には使わない

### 14-3. Success 状態の表現（緑禁則の補足）

`--success` は Navy 700 流用のためブランド色と視覚上区別できない。完了・受付中などの状態表示は**色だけに依存せず、チェックマーク等のアイコン＋テキストラベルの併記を必須**とする（WCAG 1.4.1）。

### 14-4. フォーカス・モーション・タップターゲット

- **`:focus-visible`**: すべてのインタラクティブ要素（ボタン・リンク・タブ）に `outline: 2px solid var(--blue-600); outline-offset: 2px` を標準とする（Input は v1.0 §07 のフォーカスリングを継続）
- **`prefers-reduced-motion: reduce`**: 装飾的なアニメーション（入場フェード・ホバー浮上等）は無効化するか単純な色変化に置き換える
- **タップターゲット**: モバイルで 44×44px 以上を確保（ボタン・ナビ項目・リストの行リンク）

### 14-5. レスポンシブ型スケール目安

v1.0 §03 はデスクトップ値のみのため、モバイル（〜640px）の目安を追加する。

| スタイル | Desktop | Mobile |
| --- | --- | --- |
| Display | 64 | 36〜40 |
| Heading 1 | 44 | 30〜32 |
| Heading 2 | 32 | 24〜26 |
| Heading 3 | 22 | 18〜20 |
| 電話番号（§13） | 36 | 28 |

### 14-6. 既知の表記不整合（v1.0 本文）

- §02 の Accent — Warm Orange 表には `orange-600` / `orange-400` が記載されていないが、巻末 CSS トークンには存在する（§11 は表にない Orange 600 を参照）。トークンとしては巻末 CSS が正
- `palt` の本文全適用（§03）は和文長文の可読性を下げる場合がある。長文本文では標準送りとし、見出し・リード・UI ラベルに `palt` を適用する運用を推奨

---

## 15. v1.2 実装追補 — 財団サイト（koueki.or.jp）適用で確定した規定

> **Updated 2026.07** — 全国公益支援財団 公式サイトのトップページ刷新（2026-07-12 公開）で確定した規定。§14 と同じく既存本文は改変せず、本章の規定が優先する。グループサイトへ本システムを展開する際の実装リファレンスを兼ねる。

### 15-1. ヒーロー見出しの明朝運用（Serif Display）

v1.0 §03 は Display＝Noto Sans JP 900 のみを規定していたが、ヒーローの**キャッチコピーに限り明朝（セリフ）を許可**する。

| 項目 | 規定 |
| --- | --- |
| 書体 | Noto Serif JP |
| ウェイト | **500 または 600 のみ**。⚠️ **太明朝（700 以上）は品位を損なうため禁止** |
| 字間 | +0.02em（明朝は僅かに開けて静かに組む） |
| 行送り | 1.4〜1.45 |
| 適用範囲 | ヒーローのキャッチコピー（h1）のみ。セクション見出し・本文は従来どおり Noto Sans JP |

> ✍️ **キャッチコピーの句読点禁止**: メインのキャッチコピーに読点「、」句点「。」は使わない。区切りは改行で表現する（例:「担い手が減る時代に／公益活動の歩みを止めない」）。リード文以下の通常文は句読点を使ってよい。

```css
--ff-jp-serif: "Noto Serif JP", "Hiragino Mincho ProN", "Yu Mincho", serif;
```

### 15-2. 写真上のテキスト — 白ハロ（text-halo）

写真（空などの明るい素材）を全幅背景にし、その上へ濃紺のテキストを直接載せる場合の可読性規定。v1.0 §08 の「白プレート」方式（ロゴ用）とは別に、テキストには**白いぼかしの縁取り**を用いる。

```css
/* 写真上のテキスト用: 白ハロ */
.text-halo {
  text-shadow:
    0 0 2px rgba(255, 255, 255, 0.95),
    0 0 8px rgba(255, 255, 255, 0.9),
    0 0 20px rgba(255, 255, 255, 0.75),
    0 0 40px rgba(255, 255, 255, 0.55);
}
```

併用ルール:
- テキスト側（通常は左）に `linear-gradient(to right, rgba(255,255,255,.65), rgba(255,255,255,.25) 50%, transparent)` の白フェードを重ねて補助する
- 写真上のアウトラインボタンは `background: rgba(255,255,255,.7)` ＋ `backdrop-filter: blur(4px)` を敷く（枠線だけでは沈む）
- 文字色は Navy 900 系を維持（ハロがあるため白抜き文字にしない）

### 15-3. 画像枠の比率 — 白帯（レターボックス）禁止

カード等の画像ゾーンは、**入る画像の実比率に枠を合わせて `object-fit: cover`** で敷き詰める。枠と画像の比率が合わない `contain` は上下（左右）に白帯が出て角丸との間に隙間が見えるため使わない。

- 記事タイトル画像（OGP 規格）は **1200×630** → 枠も `aspect-ratio: 1200 / 630`
- 比率不明・混在の画像は cover の軽微なトリミングで吸収する（白帯より優先）
- 読み込み中の下地は白ではなく Blue 100（#E6F3FB）等の淡色にする

### 15-4. Web 実装のトークン名（Tailwind v4）

本書のトークン名 `--blue-*` は Tailwind 標準パレットと衝突するため、**Web 実装では独立名前空間を使う**。

| 本書のトークン | Tailwind 実装名 |
| --- | --- |
| Navy 900〜500 | `navy-900`〜`navy-500` |
| Blue 600〜100（Sky Blue） | `accent-600`〜`accent-100` |
| Ink 950〜50 | `ink-950`〜`ink-50` |
| 影 sh-1〜4 | `shadow-e1`〜`shadow-e4` |

> ⚠️ **`--color-blue-*` を上書きしてはいけない**。CMS（microCMS 等）に保存済みのリッチテキストが `text-blue-800` などの Tailwind 標準クラスを直接含むため、上書きすると既存記事の見た目が黙って変わる（koueki.or.jp で実確認済みの制約）。

### 15-5. グループサイトへの適用範囲

- §10（グローバルナビ 7 カテゴリ）と §12（サブブランドカード）は**協会サイト（koueki.jp）専用**。財団等のグループサイトには適用しない
- 色（§02＋§14 補正）・タイポ（§03＋§15-1）・余白/角丸/影（§04〜05）・部品（§07、§11、§13）はグループ共通で準拠する
- 財団サイト固有のシグネチャ意匠として**「歩みの線」**を定義: 沿革タイムラインの縦線を Navy 700 → Blue 300 のグラデーションとし、最上端（未来側）を塗りのない開いた円で終える。セクション見出し先頭の短線（`.eyebrow-en::before`）も同じグラデーションで統一する

---

## 16. v1.3 追補 — 財団サイトで確定した規定

> **Updated 2026.08** — 稼働中の財団サイト（koueki.or.jp）の実装から確定した規定。§14・§15 と同じく後の章が前の章を上書きする。
> 本章は**規定だけ**を載せる。移行状況・既知の不整合といった実装側の内部メモは本書には置かず、各実装リポジトリで管理する。

### 16-1. ヒーローの標準は「写真ヒーロー」

v1.0 §08 の白基調ヒーローではなく、**写真を全幅背景に敷き、白フェード＋白ハロ（§15-2）で濃紺の文字を浮かせる**形を標準とする。

構成順: グループ帰属バッジ（Navy 800 のピル / 12px）→ 英字エイブロー → Serif キャッチ（2行・句読点なし・改行で区切る）→ リード 16px → 補足 14px → CTA 2つ（Primary ＋ Outline）。

- 背景写真は全幅・`object-cover`・優先読み込み
- 文字側の可読性は `linear-gradient(to right, rgba(255,255,255,.65), rgba(255,255,255,.25) 50%, transparent)` の白フェードで補助する
- 文字はすべて `.text-halo`。**白抜き文字にはしない**（濃紺＋ハロを維持）
- オレンジのキャッチピルは使わない（§14-2）

### 16-2. モーションは1種類だけ

サイト全体で使う動きは**入場の 1 パターンのみ**とする。

- `0.6s` / `cubic-bezier(.22,1,.36,1)` / 14px 上昇＋フェード
- ヒーロー内は 60 / 120 / 180 / 220 / 280ms のディレイで順に立ち上げる
- `prefers-reduced-motion: reduce` で無効化（§14-4）
- カードは hover 時の 2px 上昇と影（`--sh-3`）のみ。それ以外のアニメーションを追加しない

### 16-3. レイアウトの確定値

| 項目 | 値 |
| --- | --- |
| コンテナ | 最大 1240px、左右余白 20px（〜767px）→ 32px（768px 以上） |
| 読み物カラム | 一覧・表・タイムラインは 880px、記事本文は 768px |
| セクション余白 | 上下 64px（〜767px）→ 96px（768px 以上）。`--s-8` / `--s-9` に一致 |
| ヘッダー | 固定・白地・影。ロゴ高さ 56px（768px 以上 64px） |
| 角丸 | カード 16px（`--r-lg`）／アイコン枠・人物写真 10px（`--r-md`）／ボタン・バッジはピル |
| 影 | 常用は `--sh-3` のみ（カード hover とお問合せブロック）。影を足すより hover の 2px 上昇で階層を出す |

### 16-4. ボタン（財団サイト版）

| 種別 | 規定 |
| --- | --- |
| Primary | Blue 600 地・白文字・ピル / 15px / 700 / 控えめなブルーシャドウ / hover は Navy 700 |
| Navy | Navy 700 地・白文字・ピル / hover は Navy 800 |
| Outline | Navy 700 の枠線＋文字 / hover で反転（Navy 700 地・白文字） |
| 写真上の Outline | 上記に `background: rgba(255,255,255,.7)` ＋ `backdrop-filter: blur(4px)`（§15-2） |
| テキストリンク | Navy 600 / 14px / 500 ＋ hover 下線（§14-1） |

すべて**高さ 44px 以上**を確保する（§14-4）。ボタン文字は 15px / 700 に統一し、サイズ違いは作らない。

### 16-5. グローバルナビ（財団サイト版 — §10 は適用しない）

§10（ユーティリティ列＋7カテゴリ＋メガメニュー）は協会サイト専用。財団サイトは**1段・8項目・メガメニューなし**とする。

- ユーティリティ列（電話・受付時間）は持たない
- 768px 未満はハンバーガー → 全画面オーバーレイ（Navy 地・白文字・20px）
- 外部リンクは新規タブ＋`rel="noopener noreferrer"`。文中リンクには外部リンクアイコン（1.6〜1.8px ストローク）を添える

### 16-6. お問合せブロック — 電話番号は置かない（§13 を上書き）

§13 は「電話番号 → 受付時間 → フォーム」の3点セットを必須としているが、**財団サイトでは電話番号をお問合せブロックから外す**（2026-08 決定）。

| 位置 | 掲載するもの |
| --- | --- |
| ページ内のお問合せブロック | 住所 ＋ **お問合せフォーム（Primary）** ＋ **メール（Outline）**。電話番号は載せない |
| フッター | 住所・**電話番号**・メール（電話はここだけ） |

- 構造は §13 の2カラムを踏襲: 左は Navy 900 → Navy 700 のグラデ＋白文字（エイブローは Blue 300 に置き換える）、右は白地に CTA 2つ
- §13 の「電話番号 Inter 36px」規定は、電話を主動線に据えるページ（協会サイト等）でのみ有効

### 16-7. 状態色は必要になってからトークン化する

`--success` / `--warning` / `--danger` は、使う場面が来るまで実装に入れない。入れるときは §02 ではなく **§14-1 の補正値**（Warning `#B8551E` / Danger `#C24444`）を使う。

- ブランド外の色（フレームワーク標準の amber 等）を暫定で使ったまま公開画面へ広げない
- **オレンジと緑は財団サイトでは使用箇所ゼロ**。この状態を維持する（§02）

© 全国公益法人協会 · Design System v1.3 — Updated 2026.08
本書はブランド資産の使用方針をまとめたガイドラインです。Web・印刷物の制作時はかならず本書を参照してください。
