# このリポジトリでの作業ルール

デザインシステムの正典。**公開リポジトリ**であることを前提に扱う。

## 1. 規定と見本帳はセットで更新する（最重要）

`DESIGN.md`（規定の本文）を変更したら、**同じコミットで `styleguide.html`（見本帳）も更新する。**

理由: 色や数値を写す人はビジュアル版を見る。見本帳が古いと、補正前の値がそのまま実装に入る。実際に v1.0 の見本帳が残っていたために、印刷実装が AA 未達の CTA 色（`#2F8BD2`）を使い続けていた。

更新するときに触る箇所:

- [ ] `DESIGN.md` の該当章（既存本文は書き換えず**章を足す**。§03 のみ例外的に本文を改訂した）
- [ ] `styleguide.html` の `:root` トークン
- [ ] `styleguide.html` の該当セクション（該当する見本・表・サンプル）
- [ ] 版表記 **3か所**: `styleguide.html` のヒーロー（`doc-hero-eyebrow`）、同フッター、`README.md` の版の表
- [ ] `README.md` の章の表（新しい章を足したとき）

`styleguide.html` は単体の HTML（外部依存は Google Fonts のみ）。ビルド不要で、ブラウザで開けば確認できる。ロゴは `assets/logo.png` を相対パスで参照しているので、ファイルの置き場所を変えない。

## 2. 実測に基づかない値を書かない

配色のコントラスト比、型スケール、レイアウト値は、**実際の実装・ビルド済み CSS・レンダリング結果を測ってから**書く。「たぶんこう」で書いた値が独り歩きすると、実装がそれを写して固定化する。

- コントラスト比は WCAG 2.1 の式で算出した実測値を併記する
- タイポやレイアウトの値は稼働中サイトの実装から採る
- 変更後は `styleguide.html` を実際にレンダリングして目視で確認する

## 3. 公開リポジトリに置かないもの

- **人物が写った写真**（セミナー参加者・面談風景など）。肖像権の同意範囲が公開に及ばない
- 価格・キャンペーンなど**時期で古くなる販促素材**
- **未修正の不備リスト・移行の途中経過**といった実装側の内部事情 → 各実装リポジトリで管理する
- 個人の作業環境のパス、担当者名、連絡先
- 同じ画像の重複（見本帳が参照している1点だけを置く）

## 4. 版の考え方

追補方式。**後の章が前の章を上書きする。** 矛盾したら新しい章が正。章を消したり番号を振り直したりしない（他リポジトリの実装コメントが章番号で本書を参照している）。
